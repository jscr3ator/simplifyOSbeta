version: "3.9"

services:
  simplifyos:
    build: .
    container_name: simplifyos
    restart: unless-stopped
    ports:
      - "3001:3001"
    # --- FIX: Give access to the host Docker socket ---
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    # --------------------------------------------------
